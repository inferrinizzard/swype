<?xml version="1.0" encoding="utf-8"?>
<com.nuance.swype.input.settings.HackyDrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:orientation="vertical"
        android:id="@+id/drawer_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <ListView
        android:layout_gravity="start"
        android:id="@+id/list"
        android:background="@color/white"
        android:paddingTop="4dp"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:listSelector="@drawable/settings_drawer_item_background"
        android:drawSelectorOnTop="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:choiceMode="singleChoice"
        android:overScrollMode="never"/>
</com.nuance.swype.input.settings.HackyDrawerLayout>
